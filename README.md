# ipneichptc
PayPal IPN app

# build the heroku cli
```
make build-cli
```

# get the command for heroku cli alias
```
make alias-cli
```

or 

```
make $(docker run -it --rm heroku alias|dos2unix)
```

